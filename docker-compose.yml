services:
  db:
    image: postgres:17.4-alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: acade_development
    ports:
      - "5432:5432"
    volumes:
      - rails-data:/var/lib/postgresql/data

volumes:
  rails-data:
